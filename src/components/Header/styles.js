import bgImage from "../../assets/images/journey-bg.jpg";

export const styles = theme => ({
    header: {
        background: `url(${bgImage})`,
        backgroundSize: 'cover',
        backgroundPosition: 'center',
        minHeight: '100vh',
    }
})